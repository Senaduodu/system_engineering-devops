#!/usr/bin/env bash
# A Bash script that kills the 7-highlander process using SIGKILL

# Find PID(s) of 7-highlander
pid=$(pgrep -f 7-highlander)

# Kill the process forcefully if found
if [ -n "$pid" ]; then
    kill -9 "$pid"
fi
